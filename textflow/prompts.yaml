extract_names:
  |-
    Extract all person names from the following text. For each name, assign a color that matches their feeling (happy→yellow, sad→blue, angry→red, calm→green). Return ONLY a LIST of (name, colour) tuples. No other text.

    Example:
    Text:
    "Oli is sad. Nay is regal."
    Output:
    [('Oli', blue'), ('Nay', 'purple']

    Text:
    {text}

    Output:

